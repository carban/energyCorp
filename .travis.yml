language: node_js
node_js:
  - "node"
warnings_are_errors: false
before_install: 
  - cd Frontend/

install:
  - npm install
script:
  - npm run build

notificatios
deploy:
  provider: firebase
  skip_cleanup: true
  token: $FIRE_TOKEN